# JavaScript is a prototype-based language
# Multi-paradigm
# Semi-Colons are optional
# let and const are preferred to than using var
# const is immutable, meaning it can't be changed. Although, if const refers to an object,
the properties inside that object are mutable
# let is mutable just like var but let is scoped to the function it is defined in so for example, if it was declared in a for loop or a function, it would only be accessible within that functions while  

